import PostDetailTemplate from '@/components/blog/post-detail-template';

const PostDetailPage = ({ params }) => {
    return <PostDetailTemplate params={params} />;
};

export default PostDetailPage;
