import CategoriesTemplate from '@/components/category/categories-template';

export const metadata = {
    title: 'DMBI | Danh mục',
    description: 'Categories page',
};

const CategoriesPage = () => {
    return <CategoriesTemplate />;
};

export default CategoriesPage;
