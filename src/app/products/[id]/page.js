import { ProductDetailTemplate } from '@/components/product/detail';

const ProductDetailPage = ({ params }) => {
    return <ProductDetailTemplate params={params} />;
};

export default ProductDetailPage;
