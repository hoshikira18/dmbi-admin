import TokenDisplay from '@/components/common/token';
import React from 'react';
const Products = () => {
    return (
        <div>
            <TokenDisplay />
        </div>
    );
};

export default Products;
