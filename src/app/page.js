'use client';
import { Button } from '@/components/ui/button';

const HomePage = () => {
    return (
        <div>
            <h1>Home Page</h1>
            <button className="btn btn-primary bg-yellow-400 hover:bg-yellow-300">
                Click me
            </button>
        </div>
    );
};

export default HomePage;
